return {
    "nvim-neo-tree/neo-tree.nvim",
    opts = {
        window = {
            mappings = {
                ["s"] = "none", -- Originally open file in vsplit, but prefer consistent 's' for flash plugin
            }
        }
    }
}
